angular.module('bankroll', [])
    .directive('check', [function () {
        return {
            restrict: 'A',
            link: function (scope, ele, attrs) {

            }
        };
    }])
    .directive('name', [function () {
        return {
            restrict: 'A',
            link: function (scope, ele, attrs) {

            }
        };
    }])
    .directive('name', [function () {
        return {
            restrict: 'A',
            link: function (scope, ele, attrs) {

            }
        };
    }])
    .directive('name', [function () {
        return {
            restrict: 'A',
            link: function (scope, ele, attrs) {

            }
        };
    }]);